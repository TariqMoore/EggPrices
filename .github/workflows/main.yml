#name: Deploy Website

#on:
  #push:
    #branches:
      #- main: #Runes when changes are pushed to main

#jobs:
  #deploy:
    #runs-on: ubuntu-latest

    #steps:
      #- name: Checkout Repository
        #uses: actions/checkout@main
